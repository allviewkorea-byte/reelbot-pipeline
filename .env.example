# FastAPI 백엔드 주소 (Phase 2 — UI ↔ API 연결)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# ── 백엔드(travel-pipeline) ───────────────────────────────────────
# 콘티/이미지 정적 서빙 베이스 URL (storyboard image_url 생성에 사용)
# PUBLIC_BASE_URL=http://localhost:8000

# 캐릭터 일관성용 reference 이미지(front). 프론트에서 캐릭터를 안 넘기면
# 이 경로를 fallback reference로 써서 매 씬 동일 인물이 나오게 한다.
# 미설정 시 public/character-seeds/char_1779198264788/front.png 를 기본 사용.
# CHARACTER_LIBRARY_FRONT=public/character-seeds/char_1779198264788/front.png

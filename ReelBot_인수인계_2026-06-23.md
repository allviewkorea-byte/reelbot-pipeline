# ReelBot 인수인계서 — Revezen 음악 채널 (2026-06-23)

다음 claude.ai 대화가 바로 이어받을 수 있게 만든 요약. 전체 상세는 레포의 `CLAUDE.md`. 이전 인수인계서(2026-06-19, 백곰 채널)에서 이어짐 → 음악 채널 본격 구축 진행 중.

---

## 0. 시작하는 법 (다음 챗에게)

- 이 문서 먼저 읽기. 전체 SSOT는 `CLAUDE.md` (raw: `https://raw.githubusercontent.com/allviewkorea-byte/reelbot-pipeline/main/CLAUDE.md`)
- 워크플로: claude.ai(전략·한국어 작업지시서) → Claude Code(0단계 조사+커밋+푸시) → 대표가 GitHub compare로 PR 머지 → Railway/Vercel 자동배포
- 모든 PR은 누적 음악 스택 위에 쌓이는 구조. compare는 main 기준이라 이전 머지된 작업도 같이 보이는 게 정상.

---

## 1. 프로젝트 한 줄

- ReelBot = AI 기반 숏폼·음악 영상 자동 제작 파이프라인 (멀티채널).
- 채널 2개 운영:
  1. **백곰의 실화보고서** (Shorts 사연, 구독 2,270, 자동 가동 중)
  2. **Revezen** (글로벌 음악 플레이리스트 채널, 구독 4, 본격 구축 단계) ← 현재 작업 집중
- 레포 `allviewkorea-byte/reelbot-pipeline` / 프론트 Next.js→Vercel / 백엔드 FastAPI(`travel-pipeline/`)→Railway(adventurous-renewal) / Supabase / Cloudflare R2.
- AI: OpenAI(GPT 스크립트·gpt-image-1) / WaveSpeed(Z-Image·flux-kontext) / ElevenLabs(TTS) / suno API(음악, sunoapi.org) / Anthropic Claude(가사·번역)

---

## 2. 음악 채널 완료 작업 (#7~#37-B, 누적 머지됨)

### 인프라 (기존 완료)
- **#7~#16**: YouTube 업로드/OAuth, 음악 대시보드 v1, cron 자동화, 모바일 반응형, OG/PWA/파비콘

### 영상 비주얼 시스템
- **#17~#21**: Remotion 비주얼라이저 구축
  - 인트로: PLAY LIST 거대 → 좌하단 이동·축소 → 이퀄 등장 → 타이프라이터
  - mood별 색감 매핑 (시티팝→보라+시안, 카페→앰버, 이별→블루 등)
  - viz_spec 분석 시스템 (`services/music_viz_analyzer.py`)
  - GPT haiku 풍부화 + fallback

### 대시보드·UI
- **#22~#24**: 음악 채널 대시보드를 백곰 패턴과 100% 동일하게
  - 헤더 토글 (공개/AI표시/하루N개), 메트릭 4종, 트렌드 분석, 파이프라인, 마퀴
  - 9개 컴포넌트 1:1 매칭 (백곰 코드 직접 import 금지, 패턴만 차용)
- **#23**: 검토 대기 그리드 카드 (3열 PC / 2열 태블릿 / 1열 모바일)
  - 좌측 카테고리 필터 + 우측 카드 그리드
  - 카드 안에 영상·정보·작업 도구 다 박힘

### 영상 생성 자동화
- **#25 (폐기) → #26**: 수동 영상 생성 시스템
  - `[+ 수동 영상 생성]` 버튼 → 진짜 음원 1곡 → 검토 큐 정식 적재 (persist=True)
  - `[🧪 빠른 테스트 10초]` 기존 유지 (DB 영구 X)
  - 동시 1개 제한 (`_LOCK`)

### 컨셉·콘텐츠 시스템
- **#27**: 풍경 컨셉 + WHERE 라벨 + 텍스트 흰색 통일
  - 인물 80% / 거리 인물 15% / 동물 5%
  - viz_spec 확장: season, location_en, location_category, mood_category
  - WHERE 라벨 + 모든 텍스트 #FFFFFF 고정

### 운영 시스템
- **#28**: UI/UX 정리 + cron 시간 랜덤화
  - 검토 카드 분리감 강화
  - 대시보드 "오늘의 콘텐츠" → "검토 대기 미리보기"
  - cron 8/9/10시 등록 + 백엔드 스킵 로직 (오늘 이미 있으면 skip)
- **#28-A**: produce 랜덤 시간 지연 (0~120분, asyncio.to_thread 백그라운드)

### suno 비용 최적화
- **#30**: track_count 기본값 8 → 1, UI 토글 → 자유 입력창
  - 진단 결과: 영상 1개 = suno 8회 호출 = 96 크레딧 (의심됐던 비용 정체)
  - 1곡으로 변경 = 12 크레딧 (92% 절감)
- **#34-B**: suno 2클립 문제 진단·수정
  - 발견: suno 1회 호출 = 2클립 반환 → 모두 사용 → 곡수 N → 실제 2N 트랙 → 루프 느낌
  - 수정: 첫 클립만 사용 (`all_clips[:1]`), 비용 그대로, 곡수 의미 명확화

### 한국어 톤·다양성
- **#31**: 프롬프트 한국어 톤 + 도시 다양성 + 이퀄 5배
  - "Cinematic" 제거, "맑고 청량하고 밝은" 톤
  - 무드별 도시 풀 (시티팝→뉴욕/도쿄/홍콩/LA/시드니 등)
  - HF tilt 5.0 (우측 막대 활발)

### 다국어 시스템
- **#32**: 10개 언어 자막·메타데이터 + 검수 UI
  - 언어: ko + en/ja/zh/es/pt/ar/hi/th/tl/vi
  - SRT 자막 생성 (러프 동기화, suno timed_lyrics 미제공)
  - YouTube captions.insert + localizations
  - 검토 카드 `[다국어 ▼]` 패널 (탭별 수정·저장)
  - YouTube OAuth 재인증 필수 (force-ssl 스코프 추가됨)

### 정확 검수 + 운영 안정
- **#33**: 정확 검수 시스템 + WHERE 버그 + 다국어 UX + 모바일
  - 수동 재렌더 (`[재렌더]` 버튼) — 너 이미지로 영상 진짜 재합성
  - WHERE 라벨 버그 수정 (vizSpec.location_en 비어있던 캐시 문제 + fallback "City View")
  - 이미지 제거 버튼
  - 자동 번역 (영상 생성 시점)
  - 언어 탭 한국말 표시 (한국어/영어/일본어/...)
  - 모바일 pull-to-refresh

### 대시보드 미세조정
- **#34**: 이퀄 활발한 움직임 + WHERE 키우기 + 곡수 입력창
  - 이퀄 감쇠 SMOOTH_FRAMES 4 → 2 (반응 즉각)
  - WHERE 24px → 48px (#34) → 24px 원복 (#37-B에서)
  - 곡수 1~50 자유 입력 + 예상 비용·시간 표시

### 운영 가시성 (핵심 자산)
- **#36**: 작업 추적 + 파이프라인 시각화
  - music_jobs 테이블 (DB 영구 기록)
  - 백엔드 `services/music_jobs.py` (start/update/complete/fail)
  - API: `/jobs/active`, `/jobs/{id}`, `/jobs/history`
  - 대시보드 파이프라인 동적화 (보라 펄스/초록 완료/빨강 실패)
  - 검토대기 페이지 상단 진행 카드 (페이지 이동에도 유지)
  - 모바일·PC 동기화

### 메타데이터 풍부화
- **#37**: YouTube 메타데이터 + 채널 설정 페이지
  - 𝐏𝐥𝐚𝐲𝐥𝐢𝐬𝐭 시그니처 (OOOffi 스타일)
  - 본문 8섹션 자동 생성 (위치·환영·AI 명시·플랫폼·소셜·Track list·참여·해시태그)
  - 해시태그 30~50개 자동 (장르·용도·시간대·감성·위치)
  - `/music/settings` 페이지 (슬로건·이메일·소셜·AI 명시)
  - 결정적 코드 (GPT 비용 0)

### 마지막 핫픽스
- **#37-B**: 다국어 진단·수정 + WHERE 원위치 + 썸네일 압축 + 프롬프트 사이즈
  - 다국어 버그 발견·수정: 풍부한 본문 → 단일 GPT 콜에서 JSON 잘림 → ko만 남음 → 언어별 1콜 분리로 11개 언어 전부 생성
  - WHERE 폰트 48px → 24px 원복, "WHERE :" → "Where :" (W만 대문자)
  - 썸네일 413 에러 → 클라이언트 사이드 압축 (4MB → ~700KB)
  - GPT 프롬프트에 "가로형 롱폼사이즈 (1920x1080, 16:9), 4MB 이하" 명시

---

## 3. 음악 채널 운영 흐름 (완성형)

```
[매일 8/9/10시 중 첫 cron]
  → 0~120분 랜덤 지연 (#28-A)
  → 음악 헌법에서 주제 결정
  → suno N회 호출 (track_count 설정값, 기본 1)
  → 가사 자동 생성 + 다국어 번역 11개 언어 (#37-B)
  → viz_spec 분석 (season/location/mood)
  → Remotion 풀 렌더 (인트로+이퀄+곡제목+WHERE)
  → music_uploads 적재 (status=pending) + jobs 추적 (#36)
  → 검토 대기 그리드 카드 등장

[대표 검수]
  → /music/queue 진입
  → 진행 카드 + 일반 카드 동시 보임 (#36)
  → 카드에서 영상 재생, 다국어 탭 확인·수정 (#37)
  → 깨끗한 GPT 이미지 업로드 (자동 압축, #37-B)
  → [재렌더] 클릭 → 너 이미지로 진짜 영상 합성 (#33)
  → [공개 업로드] → 자막 11개 + localizations 자동 적용

[유튜브에서]
  → 시청자 지역 자동 감지
  → 자국 언어 자막·제목·설명 자동 표시
  → 𝐏𝐥𝐚𝐲𝐥𝐢𝐬𝐭 시그니처 SEO
```

---

## 4. 다음 작업 (★ 다음 챗이 이어받을 것)

### 🎯 #35 — 디자인 시스템 + 테두리 (다음 작업 1순위)

**대표 의도:**
- 채널 정체성 = PLAY LIST + WHERE 디자인
- 운영 들어가서 자유롭게 조정해야 함
- 백곰 캐릭터 시트 패턴과 비슷한 디자인 본부

**확정 사양:**

1. **대상 (각각 별도 설정):**
   - PLAY LIST (메인 로고)
   - Where : ___ 라벨 (시그니처, "Where" 표기 — W만 대문자)
   - 부제·곡 제목은 기본값 유지
2. **디자인 설정 (필수 5개 + 테두리):**
   - 폰트 패밀리 (기본 8~10개 + 커스텀 업로드 .ttf/.otf/.woff/.woff2)
   - 크기 (슬라이더)
   - 두께 (100~900)
   - 색상 (컬러피커)
   - 투명도
   - 테두리 (있음/없음, 두께, 색)
   - (그림자·배경 박스는 X — 단순 시작)
3. **실시간 미리보기:**
   - 설정 변경 시 즉시 반영
   - PLAY LIST 미리보기 박스 + Where 미리보기 박스 각각
4. **저장·적용:**
   - `channel_status.design_config` jsonb 저장
   - 다음 영상부터 자동 적용
   - 기존 영상은 [재렌더]로 반영
5. **폰트 라이선스 안내:**
   - 업로드 시 "영상 임베드 라이선스 폰트만 사용" 안내
   - 검증은 사용자 책임

작업 분량: 반나절~하루 (Remotion 동적 폰트 로딩, R2 폰트 파일 서빙, 미리보기 컴포넌트 등)

### #38 — 50곡 검증 (그 다음, 운영 직전)
- 5곡 → 10곡 → 30곡 → 50곡 단계적 검증
- 각 단계 영상 결과 확인 (이퀄·인트로·WHERE·곡 전환)
- 큰 영상 안정성 (Remotion 타임아웃 등)

### 최종 단계 — 실전 검증 + 채널 운영 시작
- 모든 시스템 한 번에 검증
- 채널 슬로건 결정 후 입력 (`/music/settings`)
- Spotify 유통 (DistroKid 가입)
- 첫 진짜 영상 공개 업로드

---

## 5. 검증 대기 (★다음 챗 첫 작업 시 확인)

대표가 검증 다 나중에 한 번에 하기로 결정. 즉시 영상 만들 필요 X.

**#35 완성 후 첫 실전 검증 시 확인 항목:**
- 파이프라인 펄스 실시간 작동 (#36)
- 진행 카드 페이지 이동 후에도 유지 (#36)
- 다국어 11개 전부 채워짐 (#37-B)
- 𝐏𝐥𝐚𝐲𝐥𝐢𝐬𝐭 시그니처 제목 + 8섹션 본문 (#37)
- "Where :" 표기 + 24px 폰트 (#37-B)
- 썸네일 업로드 압축 정상 (#37-B)
- 곡수 의미 정확 (곡수 1 = 1곡, 2N 아님) (#34-B)
- 이퀄 활발한 움직임 + 우측 끝까지 반응 (#34, #31)

---

## 6. 방향·결정 (대표 의도)

- #35 운영 들어가서 슬로건 결정 — 기본값 제안 X, 빈 칸으로 두고 사용자가 채우면 본문 자동 삽입 (PENDING)
- 대시보드가 메인 본부 — 모든 운영이 대시보드에서 이뤄져야 함
- "본 것 = 올라가는 것" — 검수의 정확성이 시스템 자산 핵심
- 검증 단계는 일회성, 운영 들어가면 자동화 100% — Cron 자동 실행으로 충분
- 글로벌 채널 정체성 — 11개 언어 자막·메타데이터로 처음부터 글로벌 시그널

---

## 7. suno API 핵심 학습 (다음 챗이 알아야 함)

- $5 = 1,000 크레딧 (sunoapi.org, suno.ai 공식 아님)
- 1회 호출 = 12 크레딧 = 2클립 반환 (변주 2개, 같은 가사·스타일)
- #34-B에서 첫 클립만 사용으로 수정 → 곡수 N = 진짜 N곡
- 곡수당 비용: 12크 × N
- POLL_TIMEOUT 600 → 900 (#30)
- 콜백 URL: `MUSIC_CALLBACK_BASE_URL` 환경변수 (`railway.internal` 사용 금지, 외부 도메인 필수)
- suno timed_lyrics 미제공 → SRT는 mix 트랙별 start_sec 기반 러프 동기화

---

## 8. 환경변수 체크 (Railway + Vercel)

### Railway (백엔드)
- `YOUTUBE_API_KEY` (공유, 백곰과 동일)
- `YOUTUBE_CHANNEL_ID_MUSIC` = `UCyVZLUzIUqtINLG1VEWw_xA`
- `MUSIC_CALLBACK_BASE_URL` = `https://reelbot-pipeline-production-94d0.up.railway.app`
- `ANTHROPIC_API_KEY` (가사·번역, 음악 채널 필수)
- `CRON_SECRET` = `rooftop-music-2026`
- `USE_REMOTION` = `on`
- (그 외 백곰 공유 변수들)

### Vercel (프론트)
- `YOUTUBE_API_KEY` (메트릭 표시용, Vercel에 별도 설정 필요)
- `YOUTUBE_CHANNEL_ID_MUSIC` = `UCyVZLUzIUqtINLG1VEWw_xA`

---

## 9. cron 설정 (cron-job.org, 대표 액션 완료)

음악 채널 produce:
- 8시: `0 8 * * *` (Asia/Seoul)
- 9시: `0 9 * * *`
- 10시: `0 10 * * *`
- URL: `https://reelbot-pipeline-production-94d0.up.railway.app/api/music/produce`
- Header: `Authorization: Bearer rooftop-music-2026`
- 첫 호출만 영상 생성, 나머지는 스킵 응답 (#28)

---

## 10. Supabase SQL 실행 완료 (대표 액션)

전부 음악 채널용:
- `music_uploads` (v1+v2)
- `music_trends`
- `music_uploads.viz_spec` jsonb (#22)
- `channel_status.track_count` int (#30)
- `music_uploads.localizations` jsonb (#32)
- `music_jobs` 테이블 (#36, 운영 가시성)
- `channel_status.channel_config` jsonb (#37, 채널 설정)

---

## 11. 불변 원칙 (계속 유지)

- 디자인 zero-diff: `globals.css @theme inline` · `components/ui/*` · tailwind 무수정, 새 색 토큰 금지
- 백곰 코드 import 금지 — 음악 채널 컴포넌트는 패턴만 차용
- 렌더/produce 코어 신중: produce-due / sayeon / make_video / music_video 의 합성 흐름은 스코프 변경만
- 음악·백곰 분리: `channel_status` 같은 공유 테이블 쓸 때 channel_id로 명확히 분리
- 신규 Supabase 컬럼 GRANT 필수
- 누적 음악 스택 머지: PR은 main 기준 compare지만 누적 스택 위에 쌓임, 마지막 머지 1번이면 전부 들어감
- 작업지시서엔 0단계 사전조사 + "PR 거부" 경고 명시
- 응답: 직접·간결 한국어, 2~3 옵션 + 명확한 추천

---

## 12. 의도된 비용 구조 (현재)

- 테스트 1곡 영상: 12 크레딧 × 1 = $0.06 + Remotion 렌더 (~5분)
- 자동 다국어 번역: 영상당 ~$0.05~0.10 (Claude API, #33 D-10)
- 메타데이터: 결정적 코드 = $0 (#37)
- 공개 업로드 시 다국어 번역: 영상당 ~$0.05~0.10 (#37-B)
- 재렌더: $0 (CPU만, 음원 재사용)
- 월 예상 (운영 시): $25~50 (영상 30개 + 다국어 + Railway·Vercel 기본)

---

## 13. 다음 챗에게 인사 (대표 스타일 학습)

- 직접·간결 한국어
- 옵션 제시 시 2~3개 + 추천 명시
- 솔직한 진단 (좋은 점·짚을 점 다 말함)
- 결과로 판단 (스크린샷·로그)
- 비개발자임을 인지 (복붙·단계별 안내)
- 명령은 항상 경로 명시 ("PowerShell에서 `C:\Users\micro\reelbot-pipeline\` 가서 실행")
- Supabase SQL은 파일경로 아닌 내용(SQL 코드) 직접 붙여줘야 함

---

## 14. 마지막 메모

- 음악 채널 백엔드 자동화 거의 완성 (#35만 남음)
- #35 완성 후 → 진짜 운영 가능 상태
- 검증은 모든 작업 끝나고 한 번에 (대표 결정)
- 대표가 슬로건·표어 미정 (운영 들어가서 결정 예정)
- suno 잔여 ~900 크레딧 (검증·초기 운영 충분)

→ 다음 챗은 **#35부터 시작**. 작업지시서는 위 4번 섹션의 확정 사양 기준으로 작성하면 됨.
